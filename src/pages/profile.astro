---
import Layout from "../layouts/Layout.astro";
import ExperienceTimeline from "../components/ExperienceTimeline.astro";
import hackathon from "../data/hackathon.json";
import speech from "../data/speech.json";
import intern from "../data/intern.json";
import { Highlighter } from "@/components/ui/highlighter";

type Item = {
  dateRange: string;
  title: string;
  location?: string;
  description?: string;
  href?: string;
  cta?: string;
};

const hackathons = hackathon as Item[];
const speeches = speech as Item[];
const internships = intern as Item[];
---

<Layout>
  <main class="mx-auto max-w-5xl px-4 py-12">
    <Highlighter client:visible action="highlight" color="#008CAF">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tight">Profile</h1>
    </Highlighter>
    <p class="mt-3 text-white/70">人が日常の中で無意識に求めているものを作りたいです。</p>

    <section class="mt-8">
      <h2 class="text-sm uppercase tracking-wider text-white/50">About</h2>
      <div class="pt-3 flex items-center gap-5">
        <h3 class="text-xl font-bold tracking-tight">Hayato Namba</h3>  <img class="w-24 h-24 rounded-full" src="/mypic.jpg" alt="My Profile" />
      </div>
      <div class="mt-3 rounded-xl border border-white/10 bg-white/5 p-5">      
        <p>
          2005年生まれ　青山学院大学学部3年　愛知県生まれ神奈川県在住です。幼い頃からものづくりとサブカルチャーが好きで、 <br />
           編集した動画を親のために作ったり、加工した画像を友達に見せびらかせたりする毎日を送っていました。<br />
           そして大学2年から現在にかけてwebプログラミングで制作活動をしています。個人、チーム開発、ハッカソンの賞獲得、技術記事の執筆、LT登壇、インターンの参加など多くの経験を経てさまざまなことを毎日学んでいます。
        </p>
      </div>
    </section>

    <section class="mt-8">
      <h2 class="text-sm uppercase tracking-wider text-white/50">Skill</h2>
      <ul class="mt-3 grid gap-3 rounded-xl border border-white/10 bg-white/5 p-5">
        <li class="">
          <h1 class="text-sm font-bold">Frontend:</h1>React, Next.js, Astro, TypeScript, Tailwind CSS, storybook, vitest, Framer Motion etc...
        </li>
        <li class="">
          <h1 class="text-sm font-bold">Backend:</h1>C#, hono, openAPI, oRPC, Prisma, Drizzle ORM
        </li>
        <li class="">
          <h1 class="text-sm font-bold">Others:</h1>Biome, Lefthook, commitLint, bun, Vercel, GitHub, Docker, TursoDB, Supabase, PostgreSQL, GitHub Actions etc...
        </li>
      </ul>
    </section>

    <section class="mt-8">
      <h2 class="text-sm uppercase tracking-wider text-white/50">Intern</h2>
      <div class="mt-3">
        <ExperienceTimeline items={internships} />
      </div>
    </section>

    <section class="mt-8">
      <h2 class="text-sm uppercase tracking-wider text-white/50">Hackathon</h2>
      <div class="mt-3">
        <ExperienceTimeline items={hackathons} />
      </div>
    </section>

    <section class="mt-8">
      <h2 class="text-sm uppercase tracking-wider text-white/50">Speech</h2>
      <div class="mt-3">
        <ExperienceTimeline items={speeches} />
      </div>
    </section>

    <p class="mt-10">
      <a href="/" class="text-white/80 underline hover:text-white">← Back to Home</a>
    </p>
  </main>
</Layout>
