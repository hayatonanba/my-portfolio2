---
import Layout from "../../layouts/Layout.astro";
import products from "../../data/products.json";
import { Highlighter } from "@/components/ui/highlighter";
---

<Layout>
  <main class="mx-auto max-w-5xl px-4 py-12">
    <Highlighter client:visible action="highlight" color="#e94709">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tight">Products</h1>
    </Highlighter>
    <p class="mt-3 text-white/70">プロダクトの一覧です。個別ページもあります。みてぇぇぇぇぇぇぇぇ</p>

    <ul class="mt-8 grid gap-4 list-none p-0">
      {products.map((p) => (
        <li class="rounded-xl border border-white/10 bg-white/5 hover:bg-white/10 transition p-5">
          <a href={`/products/${p.slug}`} class="font-semibold text-lg">
            {p.title}
          </a>
          <p class="mt-1 text-white/70">{p.summary}</p>
        </li>
      ))}
    </ul>

    <p class="mt-10">
      <a href="/" class="text-white/80 underline hover:text-white">← Back to Home</a>
    </p>
  </main>
</Layout>
